# oven-llm

A Rust library for calling LLM providers with a unified API.

## Features

- Unified API for multiple LLM providers
- Streaming support
- Tool/function calling
- Asynchronous runtime-agnostic

## Usage

See `examples/` for usage examples.
